# Lint and example note

Run this validator before submission:

```bash
npx @redocly/cli lint openapi.yaml
```

The example image fields use placeholder strings such as `BASE64_ENCODED_JPEG_IMAGE_PLACEHOLDER`. In a real request, these placeholders represent base64-encoded JPEG, PNG, or WebP images with a decoded size under the documented 5 MB limit.
